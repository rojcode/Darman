services:
  web:
    build: .
    image: darman-landing-page
    container_name: darman_container
    ports:
      - "8080:80"
    restart: always
    volumes:
      # For development: Watch file changes without rebuilding (optional)
      - .:/usr/share/nginx/html
